package lu.wealins.webia.core.service;

import javax.ws.rs.core.SecurityContext;

import lu.wealins.common.dto.webia.services.RelaunchStepRequest;
import lu.wealins.common.dto.webia.services.RelaunchStepResponse;

public interface RelaunchStepUtilityService {

	/**
	 * Relaunch the step with the complete action.
	 * 
	 * @param context The security context.
	 * @param relaunchStepRequest The relaunch step request.
	 * @return The relaunch step response.
	 */
	RelaunchStepResponse relaunchStep(SecurityContext context, RelaunchStepRequest relaunchStepRequest);
}
