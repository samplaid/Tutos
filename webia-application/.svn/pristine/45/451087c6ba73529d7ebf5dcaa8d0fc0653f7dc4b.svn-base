package lu.wealins.webia.core.service;

import lu.wealins.webia.ws.rest.request.PolicyEventUpdateServiceRequest;

public interface DaliService {

	/***
	 * Init plus value before and after 27/09/2017 of dali policy event.
	 * 
	 * @param eventUpdate
	 */
	String initEventPlusValueBeforeAndAfter(PolicyEventUpdateServiceRequest eventUpdate);

	/***
	 * Init plus value before and after 27/09/2017 of dali policy event by is
	 * Transaction Id.
	 * 
	 * @param eventUpdate
	 */
	String initEventPlusValueBeforeAndAfter(Long transactionTaxId);
}
