/**
 * 
 */
package lu.wealins.webia.core.service;

import lu.wealins.common.dto.liability.services.AssetManagerStrategyDTO;

/**
 * This interface is the mediator between the liability module and the webia module.
 * 
 * @author oro
 *
 */
public interface AssetManagerStrategyService {

	/**
	 * Create or update the asset manager strategy
	 * 
	 * @param strategy the strategy
	 * @return an identifiable object.
	 */
	AssetManagerStrategyDTO save(AssetManagerStrategyDTO strategy);

	/**
	 * Set the identity of asset manager strategy
	 * 
	 * @param managerStrategy the asset manger
	 */
	void setId(AssetManagerStrategyDTO managerStrategy);
}
