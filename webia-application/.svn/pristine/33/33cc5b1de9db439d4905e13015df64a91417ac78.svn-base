package lu.wealins.webia.core.service;

import lu.wealins.common.dto.webia.services.AppFormDTO;

public interface LiabilityAnalysisService {

	/**
	 * Enrich the analysis step with liability data.
	 * 
	 * @param analysis The analysis step.
	 * @return The enriched analysis step
	 */
	AppFormDTO enrichAnalysis(AppFormDTO analysis);
}
