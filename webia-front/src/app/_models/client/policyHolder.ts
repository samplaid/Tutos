import {FullClient} from './fullClient';

export class PolicyHolder extends FullClient {

    usufructuary: boolean;
    bareOwner: boolean;
    deathSuccessor: boolean;
    lifeSuccessor: boolean;

    displayNumber: number;

}