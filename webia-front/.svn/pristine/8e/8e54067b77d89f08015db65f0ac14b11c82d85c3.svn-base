import { PolicyAgentShare, AgentLite, UoptDetails } from '../../_models';
import { PolicyValuation } from '../../_models/policy/policy-valuation';

export class BrokerChange  {

	brokerRefContract:string;
	broker: PolicyAgentShare;
	subBroker: PolicyAgentShare;
	brokerContact: PolicyAgentShare;
	mailToAgent: AgentLite;
	// sending rules
	category: UoptDetails;
	policyValuation: PolicyValuation;
	changeDate?: string;
	policyId?: string;
	workflowItemId?: number;
    readonly type ="BROKER_CHANGE";
}
