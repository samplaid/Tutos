<section subject="communication-and-specificities">
    <div class="row" >
        <div class="title col-md-5">Communication & Specificities</div>
    </div>

    <div class="row" *ngIf="displaySendingRulesLine">
        <label class="col-md-2 text-right">Sending Rules</label>
        <div class="col-md-3">{{category?.description}}</div>
        <span *ngIf="mailToAgent">
            <label class="col-md-2 text-right">Agent</label>
            <div class="col-md-3">{{ mailToAgent.name}} - {{ mailToAgent.agtId}}</div>
        </span>
    </div>

    <div class="row" *ngIf="displayFaxAndMandateLine">
        <label class="col-md-2 text-right">Order by FAX</label>
        <div class="col-md-3">
            <span>{{ orderByFax ?'Yes':'No'}}</span>
            <span class="marginHorizontal1">{{ mandatoAllIncasso ? "Mandato all'incasso":''}}</span>
        </div>
        <span>
            <label class="col-md-4 text-right">Mandate of transmission except broker</label>
            <div class="col-md-2">
                <span>{{ exMandate ?'Yes':'No'}}</span>
            </div>
        </span>
    </div>

    <div class="row form-horizontal" *ngIf="displayNonSurrenderLine">
        <label class="form-group col-md-2 text-right nopadding" *ngIf="(nonSurrenderClauseDate | wdate)">Non-Surrender Clause Date</label>
        <div class="col-md-2 marginLeft10" *ngIf="(nonSurrenderClauseDate | wdate)">{{nonSurrenderClauseDate | wdate }}</div>
        <div class="col-md-4" *ngIf="!(nonSurrenderClauseDate | wdate)"> </div>
        <div class="col-md-2 col-md-offset-1">{{scudo ? 'Scudo':''}}</div>
    </div>

    <div class="row" *ngIf="displayPledgeLine">
        <label class="col-md-2 text-right">Pledge</label>
        <div class="col-md-8">{{ assignmentPledge ? 'Yes - ' + ((assignmentPledgeDetails) ? assignmentPledgeDetails : '') :'No' }}</div>
    </div>

</section>