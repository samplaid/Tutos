/**
 * 
 */
package lu.wealins.liability.services.core.validation;

import java.util.Set;

import lu.wealins.common.dto.liability.services.ClientContactDetailDTO;

/**
 * This interface provides method to validate the client contact.
 * 
 * @author oro
 *
 */
public interface ClientContactDetailValidationService {

	/**
	 * If the client country has been changed, assert the date of change to be filled in.
	 * 
	 * @param clientContact the client contact.
	 * @return a set of errors
	 */
	Set<String> validateCountryDateOfChange(ClientContactDetailDTO clientContact);
}
