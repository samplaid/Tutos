package lu.wealins.webia.services.core.service.validations;

import java.util.Collection;
import java.util.List;

import lu.wealins.common.dto.webia.services.AppFormDTO;
import lu.wealins.common.dto.webia.services.BenefClauseFormDTO;

public interface DeathClausesValidationService {

	/**
	 * Validate the death clauses.
	 * 
	 * @param appForm
	 *            the {@link AppFormDTO}.
	 * @param errors
	 *            the {@link List} of errors to populate
	 */
	void validateDeathClauses(Integer policyHolderSize, String productCountry, Integer lives,
			Collection<BenefClauseFormDTO> benefClauses, List<String> errors);// (
																				// AppFormDTO
	// appForm,
	// List<String>
	// errors);

}
