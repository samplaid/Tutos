package lu.wealins.webia.services.core.persistence.entity;

import static org.junit.Assert.assertTrue;

import java.math.BigDecimal;
import java.util.Date;

import org.junit.Test;

public class SapAccountingEntityTest {
	private final static int LINE_LENGTH = 500;

	@Test
	public void testBuildLineForFileLength() {
		SapAccountingEntity entity = new SapAccountingEntity();
		entity.setCreationDate(new Date());
		entity.setChangeRate(new BigDecimal(0));
		//assertTrue(entity.buildLineForFile().length() == LINE_LENGTH);
	}

}
