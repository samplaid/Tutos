package lu.wealins.webia.services.core.service.validations.impl;

import org.springframework.stereotype.Service;

import lu.wealins.webia.services.core.service.validations.StepValidationBuilderService;

@Service
public class StepValidationBuilderServiceImpl implements StepValidationBuilderService {

	
	// private List<WebiaStepValidationService> validationServicesByOperation;
	//
	// @Autowired
	// public void setValidationServicesMap(List<WebiaStepValidationService> services) {
	// validationServicesByOperation = services.stream().collect(Collectors.toList());
	// }
	//
	// @Override
	// @SuppressWarnings("unchecked")
	// public List<String> validateBeforeComplete(StepDTO step) {
	// Assert.notNull(step);
	//
	// List<String> errors = new ArrayList<>();
	//
	// List<WebiaStepValidationService> validationServices = validationServicesByOperation.stream().filter(x -> x.needValidateBeforeComplete(step)).collect(Collectors.toList());
	// validationServices.forEach(x -> errors.addAll(x.validateBeforeComplete(step)));
	//
	// return errors;
	// }
	//
	//
	// @SuppressWarnings("unchecked")
	// @Override
	// public List<String> validateBeforeSave(StepDTO step) {
	// Assert.notNull(step);
	//
	// List<String> errors = new ArrayList<>();
	//
	// List<WebiaStepValidationService> validationServices = validationServicesByOperation.stream().filter(x -> x.needValidateBeforeSave(step)).collect(Collectors.toList());
	// validationServices.forEach(x -> errors.addAll(x.validateBeforeSave(step)));
	//
	// return errors;
	// }

}
